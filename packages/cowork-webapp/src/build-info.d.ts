// Globals injected by vite.config.ts → `define`. Surfaced in the settings
// page so users can confirm which build their browser is actually serving
// — vs the bundle they pushed but haven't reloaded.
declare const __BUILD_TIME__: string;
declare const __BUILD_COMMIT__: string;
